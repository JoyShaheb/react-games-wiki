import React from "react";

const Home = () => {
  return (
    <div>
      <button onClick={() => console.log("Hello")} className="">
        click to toggle theme
      </button>
    </div>
  );
};

export default Home;
